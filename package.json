{
  "name": "brainblocks-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node .",
    "start:dev": "NODE_ENV=development node .",
    "clean": "node ./clean.js",
    "clean:dev": "NODE_ENV=development node ./clean.js",
    "recover": "node ./recover.js",
    "recover:dev": "NODE_ENV=development node ./recover.js",
    "populate-precache": "node ./populate_precache.js",
    "db-setup": "./scripts/db-setup.sh $1 $2",
    "test": "npm run lint && npm run flow-typed && npm run flow && npm run jest",
    "flow-typed": "flow-typed install",
    "flow": "flow",
    "lint": "eslint server/ test/ *.js",
    "checkdb": "NODE_ENV=test babel-node ./testdb.js",
    "jest": "npm run db-setup brainblocks-test postgres && DB_HOST=postgres DB_PORT=5432 jest test --env=node --no-cache --coverage --verbose --runInBand --silent",
    "jest-local": "npm run db-setup brainblocks-test localhost && jest test --env=node --no-cache --coverage --verbose --runInBand --silent"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqp-delay.node": "^0.2.0",
    "amqplib": "^0.5.2",
    "async-exit-hook": "^2.0.1",
    "base-64": "^0.1.0",
    "big-integer": "^1",
    "child-process-promise": "^2.2.1",
    "exit-hook": "^1.1.1",
    "express": "^4.16.2",
    "jsonwebtoken": "^8.1.0",
    "node-fetch": "^2.2.0",
    "node-loggly-bulk": "^2.2.1",
    "pg": "^7.4.1",
    "raiblocks-client": "^1.0.0",
    "raven": "^2.4.1",
    "swagger-ui-express": "^4.0.1",
    "uuid": "^3.1.0",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "grumbler-scripts": "^2.0.63",
    "rabbot": "^1.1.0",
    "request": "^2.83.0",
    "request-promise": "^4.2.2",
    "flow-bin": "^0.69.0",
    "jest": "^22.4.3",
    "nock": "^9",
    "node-fetch": "^2",
    "qs": "^6",
    "pgtest": "^0",
    "puppeteer": "^1",
    "wtfnode": "^0"
  }
}
